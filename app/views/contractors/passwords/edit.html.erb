<% provide(:title, 'パスワード再設定') %>

<div class="flex justify-center items-center" >
  <div class="w-1/3">
    <h2 class="text-3xl pb-10 font-bold">パスワードを変更(受注者)</h2>

    <%= render 'shared/password_edit_form_fields', resource: resource, resource_name: resource_name %>

    <%= render "contractors/shared/links" %>
  </div>
</div>